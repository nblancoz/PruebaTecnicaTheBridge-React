import React from "react";
import "./Header.scss";

const Header = () => {
  return (
    <nav className="navBar">
      <p className="navTitle">Prueba Técnica</p>
    </nav>
  );
};

export default Header;
